# Cloudflare Pages — редиректы.
# Все редиректы живут в functions/_middleware.js (edge выполняется раньше
# _redirects, поэтому держать их в двух местах — только путаница):
#   • rss/feed → /rss/ru.xml
#   • выбор локали на /
#   • пути без префикса локали → /ru/…
#   • нормализация legacy тег-слагов (регистр + пробел)
# www→apex и HTTPS — настройками зоны Cloudflare.
